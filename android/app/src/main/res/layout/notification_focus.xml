<?xml version="1.0" encoding="utf-8"?>
<!--
  The session card, drawn to the spec rather than to Android's default template.

  The Chronometer is the whole reason this is a custom layout: with `countDown`
  and a base of the session's end instant, the system ticks it itself, so
  nothing in the app wakes to move the clock. A TextView here would mean pushing
  an update every second.

  Frozen swaps the chronometer for `time_static`, because a system-ticked
  countdown cannot be paused and would keep falling on a held session.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:paddingTop="12dp"
    android:paddingBottom="12dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <ImageView
            android:id="@+id/ada"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:layout_marginEnd="13dp"
            android:contentDescription="@null"
            android:src="@drawable/ada_stage_0" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/task"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:maxLines="1"
                android:ellipsize="end"
                android:textSize="16sp"
                android:textStyle="bold"
                android:textColor="#FFFFFFFF" />

            <TextView
                android:id="@+id/subtitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="1dp"
                android:maxLines="1"
                android:ellipsize="end"
                android:textSize="11sp"
                android:letterSpacing="0.06"
                android:textColor="#8B8798" />
        </LinearLayout>

        <Chronometer
            android:id="@+id/time"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:textSize="26sp"
            android:textStyle="bold"
            android:fontFamily="monospace"
            android:textColor="#FFFFFFFF" />

        <TextView
            android:id="@+id/time_static"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:textSize="26sp"
            android:textStyle="bold"
            android:fontFamily="monospace"
            android:textColor="#9FD6EF"
            android:visibility="gone" />
    </LinearLayout>

    <ProgressBar
        android:id="@+id/rail"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="6dp"
        android:layout_marginTop="12dp"
        android:max="100"
        android:progressDrawable="@drawable/rail_progress" />
</LinearLayout>
